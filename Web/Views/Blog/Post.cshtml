@model MarkdownBlog.Net.Web.Models.Post
@{ ViewBag.Title = PostHelpers.PostTitle(Model.Metadata); }

<article>
    @Html.Raw(Model.Body)
</article>

@Html.Partial("Disqus", Model.Disqus)

@section asides
{
    @Html.Partial("Asides", Model)
}
