@model MarkdownBlog.Net.Web.Models.PostMetadata

@if (Model != null) {
    <a href="/blog/@Model.Slug">@Model.Title - <i>Posted @Model.PublishDate by @Model.Author</i></a>
<p>@Model.ShortDescription</p>
}
